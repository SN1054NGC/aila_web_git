# ============================================================
#  АЙЛА — локальная версия. Минимальный набор (без torch, без моделей)
#  Этого достаточно для чата, поиска по Excel и браузерного GUI.
# ============================================================
fastapi>=0.110
uvicorn[standard]>=0.24
httpx>=0.25
python-multipart>=0.0.6
pandas>=2.0
openpyxl>=3.1
numpy>=1.24

# ============================================================
#  ГОЛОС (проверено в работе)
# ============================================================

# Распознавание речи офлайн, потоковое (~45 МБ модель на язык)
vosk>=0.3.45

# Синтез речи ONNX, без torch — основной движок для слабого ПК
piper-tts>=1.8.0

# Синтез Silero (torch) — альтернативный движок, включается AILA_TTS=silero
# torch>=2.0
# torchaudio>=2.0

# Семантический поиск вместо BM25 (тяжелее: тянет torch и chromadb)
# chromadb>=0.4.22,<0.5
# sentence-transformers>=2.2.0,<3.0

# Загрузка моделей из интернета ОДИН раз (потом можно удалить)
# huggingface_hub>=0.19
